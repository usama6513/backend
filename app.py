# Import the FastAPI app from src/main.py
from src.main import app

# This file serves as the entry point for Hugging Face Spaces
# The app is already configured in src/main.py with lifespan events and middleware